# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  protogen:
    desc: Generate protobuf code
    cmds:
    - rm -rf protogen
    - rm -rf docs
    - buf dep update
    - buf generate
    - go mod tidy

    silent: true
